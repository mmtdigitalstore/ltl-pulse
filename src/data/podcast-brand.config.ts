/**
 * Two labels only:
 * - navLabel → navigation and footer
 * - name → everywhere else (page titles, cards, promos, links)
 *
 * LTL Conversations is the show; /podcast is the route.
 */
export const PODCAST_BRAND = {
  navLabel: "Podcast",
  name: "LTL Conversations",
} as const;
